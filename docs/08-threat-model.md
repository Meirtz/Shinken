# Isolation & capability note

This document moved to [`design/threat-model.md`](design/threat-model.md) and was reframed as a
runtime design note (isolation + capability scoping), not a threat model.

The old path is kept as a compatibility stub for external links.
